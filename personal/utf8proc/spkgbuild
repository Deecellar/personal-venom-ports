# description	: UTF8 support library
# homepage	: 
# depends	: 

name=utf8proc
version=2.6.1
release=1
noextract=""
backup=""
source="https://github.com/JuliaStrings/utf8proc/archive/refs/tags/v$version.tar.gz"

build() {
	mkdir $PKG/usr -p
	cd $name-$version
	make
	make install prefix=$PKG/usr
}
